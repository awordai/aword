[aword]
in_production = False

[cache]
provider = edge
add_summaries = true
db_file = res/cache.db

[vector]
provider = qdrant
local_db = test/res/local.qdrant
collection_name = test-collection

# if qdrant_url is defined we'll use it
# url =

[embedding]
model_name = text-embedding-ada-002
embedding_chunk_size = 400

[completion]
model_name = gpt-3.5-turbo
n_chunks_for_context = 8
system_prompt_file = res/system-prompt-chat.txt
